generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Acá le enseño a Prisma qué es un Contacto
model Contacto {
  id        Int      @id @default(autoincrement())
  nombre    String
  email     String
}